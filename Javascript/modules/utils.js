export const addToNums = (a, b) => {
  return a + b;
};
export const mulToNums = (a, b) => {
  return a * b;
};

const getDetails = () => {
  console.log("log from util");
};

export default getDetails;
